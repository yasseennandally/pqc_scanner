# pqc-scanner

A small tool that scans TLS endpoints, extracts certificate/crypto details, and produces a PQC-readiness report.

## Run locally (initial)
This repo will contain:
- `backend/` FastAPI service (scanner API)
- `frontend/` Next.js dashboard
- `infra/` Docker + deployment files
- `docs/` product + technical notes

### Quick start
(coming next)
